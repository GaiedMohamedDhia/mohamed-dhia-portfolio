import { notFound } from "next/navigation";
import { projects } from "../../../data/projects";
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <main className="case-page"><nav><a href="/">← Portfolio</a><span>MDG / CASE STUDY</span></nav><header><p>SELECTED PROJECT</p><h1>{p.title}</h1><div>{p.technologies.map(t=><span key={t}>{t}</span>)}</div></header><section><h2>Overview</h2><p>{p.overview||p.description}</p></section>{p.features?.length&&<section><h2>Features</h2><ul>{p.features.map(f=><li key={f}>{f}</li>)}</ul></section>}<aside>Unavailable sections are intentionally hidden until verified technical details, screenshots, architecture, challenges, repository links, and demos are provided.</aside></main>}
